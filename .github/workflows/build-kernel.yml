# .github/workflows/build-kernel.yml
name: Build Debian Linux Kernel

on:
  workflow_dispatch:

  schedule:
    - cron: "28 10 * * 1"

permissions:
  contents: write

concurrency:
  group: build-debian-kernel
  cancel-in-progress: false

env:
  KERNEL_REPO: https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git
  DEBIAN_FRONTEND: noninteractive

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 180

    steps:
      - name: Free disk space
        run: |
          set -euxo pipefail
          df -h
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/lib/android \
            /usr/local/share/boost "${AGENT_TOOLSDIRECTORY:-}" || true
          df -h

      - name: Install build dependencies
        run: |
          sudo dpkg --add-architecture "$(dpkg --print-architecture)"
          sudo apt-get update
          sudo apt-get install -y build-essential bc kmod cpio flex libncurses-dev \
            libssl-dev libelf-dev bison dwarves debhelper rsync libdw-dev

      - name: Determine latest stable kernel version
        id: version
        run: |
          set -euo pipefail
          latest_tag="$(
            git ls-remote --tags "$KERNEL_REPO" \
              | awk '{print $2}' \
              | sed 's#refs/tags/##' \
              | grep -Ev '\^\{\}' \
              | grep -Ev -- '-rc' \
              | grep -E '^v[0-9]+\.[0-9]+(\.[0-9]+)?$' \
              | sort -V \
              | tail -n 1
          )"

          # Strict format assertion to prevent injection from malformed tags
          if ! [[ "$latest_tag" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::Unexpected tag format: '$latest_tag'"
            exit 1
          fi

          echo "tag=$latest_tag" >> "$GITHUB_OUTPUT"
          echo "Latest stable kernel tag: $latest_tag"

      - name: Clone stable kernel
        run: |
          set -euxo pipefail
          git clone --depth=1 --branch "${{ steps.version.outputs.tag }}" "$KERNEL_REPO" /tmp/linux

      - name: Configure kernel
        run: |
          set -euxo pipefail
          cd /tmp/linux
          make defconfig
          
          scripts/config --disable DEBUG_INFO
          scripts/config --disable DEBUG_INFO_DWARF5
          scripts/config --enable DEBUG_INFO_NONE
          scripts/config --disable MODULE_SIG
          scripts/config --disable MODULE_SIG_ALL
          scripts/config --set-str SYSTEM_TRUSTED_KEYS ""
          scripts/config --set-str SYSTEM_REVOCATION_KEYS ""
          scripts/config --enable TCP_CONG_ADVANCED
          scripts/config --enable TCP_CONG_BBR
          scripts/config --enable DEFAULT_BBR
          scripts/config --disable DEFAULT_CUBIC
          scripts/config --set-str DEFAULT_TCP_CONG bbr
          scripts/config --set-val NR_CPUS 64
          scripts/config --disable MAXSMP
          scripts/config --disable SCHED_MC_PRIO
          scripts/config --disable NUMA
          scripts/config --disable NUMA_BALANCING
          scripts/config --disable X86_5LEVEL
          scripts/config --enable NO_HZ_IDLE
          scripts/config --disable NO_HZ_FULL
          scripts/config --enable HZ_250
          scripts/config --disable HZ_1000
          scripts/config --enable PREEMPT_VOLUNTARY
          scripts/config --disable PREEMPT
          scripts/config --enable HYPERVISOR_GUEST
          scripts/config --enable PARAVIRT
          scripts/config --enable PARAVIRT_SPINLOCKS
          scripts/config --enable KVM_GUEST
          scripts/config --enable PARAVIRT_CLOCK
          scripts/config --enable VIRTIO
          scripts/config --enable VIRTIO_PCI
          scripts/config --enable VIRTIO_PCI_LEGACY
          scripts/config --enable VIRTIO_BLK
          scripts/config --enable VIRTIO_NET
          scripts/config --module VIRTIO_CONSOLE
          scripts/config --enable EXT4_FS
          scripts/config --module XFS_FS
          scripts/config --disable DRM
          scripts/config --disable FB
          scripts/config --disable LOGO
          scripts/config --disable VGA_CONSOLE
          scripts/config --enable SERIAL_8250_CONSOLE
          make olddefconfig

          grep -qE '^CONFIG_TCP_CONG_BBR=(y|m)' .config || { echo "::error::BBR missing"; exit 1; }
          grep -q '^CONFIG_DEFAULT_BBR=y' .config || { echo "::error::DEFAULT_BBR not selected"; exit 1; }
          grep -q '^CONFIG_DEFAULT_TCP_CONG="bbr"' .config || { echo "::error::DEFAULT_TCP_CONG is not bbr"; exit 1; }
          grep -qE "^CONFIG_VIRTIO_NET=(y|m)" .config || { echo "::error::VIRTIO_NET missing"; exit 1; }
          grep -qE "^CONFIG_VIRTIO_BLK=(y|m)" .config || { echo "::error::VIRTIO_BLK missing"; exit 1; }
          grep -qE "^CONFIG_XFS_FS=(y|m)" .config || { echo "::error::XFS_FS missing"; exit 1; }
          grep -q "^CONFIG_NR_CPUS=64" .config || { echo "::error::NR_CPUS is not 64"; exit 1; }
          grep -q "^CONFIG_NO_HZ_IDLE=y" .config || { echo "::error::NO_HZ_IDLE missing"; exit 1; }
          grep -q "^CONFIG_NO_HZ_FULL is not set" .config || true

      - name: Build kernel
        run: |
          set -euxo pipefail
          cd /tmp/linux
          make -j"$(nproc)" scripts
          chmod +x scripts/basic/fixdep || true
          make -j"$(nproc)" bindeb-pkg \
            KDEB_PKGVERSION="$(make -s kernelrelease)-1"
            
      - name: Get kernel release
        id: krel
        run: |
          set -euo pipefail
          cd /tmp/linux
          echo "release=$(make -s kernelrelease)" >> "$GITHUB_OUTPUT"

      - name: Collect and verify Debian packages
        run: |
          set -euxo pipefail
          mkdir -p artifacts
          find /tmp -maxdepth 1 -type f -name "linux-*.deb" -exec cp -v {} artifacts/ \;

          ls -lh artifacts/
          find artifacts -name "linux-image-*.deb" | grep -q . || \
            { echo "::error::linux-image package missing"; exit 1; }

      - name: Verify BBR module in image package
        run: |
          set -euxo pipefail
          image_deb="$(find artifacts -name "linux-image-*.deb" | head -1)"
          mkdir -p /tmp/img-check
          dpkg-deb -x "$image_deb" /tmp/img-check

          find /tmp/img-check/lib/modules -type f \( -name "tcp_bbr.ko" -o -name "tcp_bbr.ko.*" \) | grep -q . || \
            { echo "::error::BBRv1 module missing"; exit 1; }

      - name: Generate README
        run: |
          set -euo pipefail
          kernel_release="${{ steps.krel.outputs.release }}"
          build_date="$(date -u '+%Y-%m-%d %H:%M UTC')"
          export kernel_release build_date
          envsubst '$kernel_release $build_date' > artifacts/README.md <<'EOF'
            # Debian Kernel Build
            | Item | Value |
            |------|-------|
            | Kernel Version | `$kernel_release` |
            | Build Date | $build_date |
            | Base Config | Debian amd64 |
          EOF
          
      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: "Debian Kernel ${{ steps.krel.outputs.release }}"
          path: artifacts/*
          if-no-files-found: error
          retention-days: 14

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.krel.outputs.release }}-${{ github.run_number }}
          name: "Debian Kernel ${{ steps.krel.outputs.release }}"
          files: artifacts/*
          body_path: artifacts/README.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
