# 已审阅的 CachyOS BBRv3 topic 分支锚点（供应链投毒防线）。
#
# 每行格式：<分支名> <已审阅的 tip SHA>
# 空行以及 # 开头的行会被忽略。
#
# 构建流程只接受这里记录的 SHA。CachyOS 会 force-push 这些 topic 分支；没有
# 这份锚点时，上游任何一次推进都会在 3 小时内自动进入构建并发布给所有用户。
#
# 分支前进后，check 作业会失败并打印 compare 链接。请先人工审阅 diff，确认：
#   1. 只触及 BBRv3 相关代码，没有在 TCP 主路径里夹带与 BBR 无关的逻辑；
#   2. net/ipv4/Makefile 只是追加 obj-$(CONFIG_TCP_CONG_BBR3) 一行；
#   3. 提交作者与内容与上游 BBRv3 补丁集一致。
# 确认无误后，把该行的 SHA 更新为新的 tip 并提交。
#
# 内核进入新的大版本系列时，需要在这里新增一行（例如 7.3/bbr3）。
# 首次填入同样必须先人工审阅。

7.2/bbr3 7235d70addc6fb9f9b974aa07930d3fd87ca6311
